"use strict";

const React = require('react');

const Home = React.createClass({
    render: () => {
        return (
            <div className="jumbotron">
                <h1>Plural Portal</h1>
                <p>Course react, redux</p>
            </div>
        )
    }
});

module.exports = Home;